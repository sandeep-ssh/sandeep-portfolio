name: AI Project Summary

on:
  push:
    paths:
      - data/projects.json

jobs:
  summarize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "AI summary will be added later"
